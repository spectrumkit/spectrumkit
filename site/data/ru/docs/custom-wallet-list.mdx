---
title: Пользовательский список кошельков
description: Настройка списка кошельков
---

# Пользовательский список кошельков

## Настройка списка кошельков

Вы можете импортировать индивидуальные кошельки из `'@spectrumkit/spectrumkit/wallets'` вместе с функцией `connectorsForWallets`, чтобы создать свой собственный список кошельков с их необходимыми соединителями. Таким образом, у вас будет полный контроль над тем, какие кошельки отображать и в каком порядке.

Например, вы можете выбрать отображение только Rainbow вместе с универсальными резервными кошельками.

```tsx
import { connectorsForWallets } from '@spectrumkit/spectrumkit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@spectrumkit/spectrumkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My RainbowKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

Вы можете затем передать свои соединители в Wagmi `createConfig`.

```tsx
import { connectorsForWallets } from '@spectrumkit/spectrumkit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <RainbowKitProvider {...etc}>
        {/* Your App */}
      </RainbowKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### Встроенные кошельки

Следующие кошельки предоставляются через объект `wallet`.

#### Универсальные кошельки

Следующие кошельки ориентированы на универсальные методы подключения, а не на конкретные приложения. В результате рекомендуется всегда включать эти кошельки.

##### WalletConnect

Это резервный вариант кошелька, предназначенный для кошельков на базе WalletConnect, которые не были предоставлены другим кошельком в списке.

**Рекомендуется всегда включать этот кошелек в список, чтобы обеспечить поддержку всех кошельков на базе WalletConnect.**

```tsx
import { walletConnectWallet } from '@spectrumkit/spectrumkit/wallets';
```

##### Встроенный кошелек

Это резервный вариант кошелька, предназначенный для ситуаций, когда `window.ethereum` существует, но не был предоставлен другим кошельком в списке. Этот кошелек автоматически скроет себя из списка, когда резерв не требуется или если доступного встроенного кошелька нет.

**Рекомендуется всегда включать этот кошелек в список, чтобы обеспечить поддержку всех встроенных кошельков.**

```tsx
import { injectedWallet } from '@spectrumkit/spectrumkit/wallets';
```

##### Безопасный кошелек

Для dApps, которые поддерживают Safe Apps, предоставляя пользователям возможность легко подключаться к их Safe vault или мульти-сиг, включите опцию `safeWallet` для автоматического подключения. Опция Safe будет отображаться только в браузерной среде Safe Wallet.

```tsx
import { safeWallet } from '@spectrumkit/spectrumkit/wallets';
```

### Конкретные кошельки

Следующие кошельки предоставляются через объект `wallet` (в алфавитном порядке).

#### 1inch Wallet

```tsx
import { oneInchWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Backpack

```tsx
import { backpackWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Базовый аккаунт (ранее Coinbase Wallet)

Этот коннектор для кошелька поддерживает Базовый аккаунт, который является развитием Coinbase Smart Wallet. Он обеспечивает беспрепятственную аутентификацию и улучшенные функции, такие как подаккаунты и разрешения на расходы.

Настройки конфигурации соответствуют [ссылочному руководству SDK Базового аккаунта](https://docs.base.org/base-account/reference/core/createBaseAccount).

```tsx
import { baseAccount } from '@spectrumkit/spectrumkit/wallets';

// Configure attribution (choose one approach)
baseAccount.preference = {
  attribution: { auto: true }, // Smart Wallet generates origin-based identifier
};

// Or use custom attribution
baseAccount.preference = {
  attribution: { dataSuffix: '0x1234567890abcdef' }, // 16-byte hex identifier
};

// Configure sub-accounts
baseAccount.subAccounts = {
  creation: 'on-connect', // or 'manual'
  defaultAccount: 'sub', // or 'universal'
  funding: 'spend-permissions', // or 'manual'
};

// Configure paymaster URLs for gasless transactions
baseAccount.paymasterUrls = {
  [base.id]: 'https://api.developer.coinbase.com/rpc/v1/base/...',
};
```

#### BeraSig

```tsx
import { berasigWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Лучший кошелек

```tsx
import { bestWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Bifrost Wallet

```tsx
import { bifrostWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Binance Web3 Wallet

```tsx
import { binanceWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Wallet Bloom

```tsx
import { bloomWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Brave кошелек

```tsx
import { braveWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Bybit кошелек

```tsx
import { bybitWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Coin98 кошелек

```tsx
import { coin98Wallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Compass кошелек

```tsx
import { compassWallet } from '@spectrumkit/spectrumkit/compassWallet';
```

#### Основа

```tsx
import { coreWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Кошелек CTRL

```tsx
import { ctrlWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Заря кошелек

```tsx
import { dawnWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Энкрипт кошелек

```tsx
import { enkryptWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Frame кошелек

```tsx
import { frameWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Граница кошелек

```tsx
import { frontierWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Кошелек ворот

```tsx
import { gateWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Кошелек Gemini

Этот умный коннектор кошелька позволяет безупречно аутентифицироваться через ключи на keys.gemini.com — без загрузок, расширений или API ключей. Просто работает из коробки.

Параметр `appName` необходим для идентификации вашего dApp, а `appIcon` является необязательным для брендинга опыта подключения.

```tsx
import { geminiWallet } from '@spectrumkit/spectrumkit/wallets';

const gemini = geminiWallet({
  appName: 'My App',
  appIcon: 'https://my-app.com/logo.png', // Optional
});
```

#### imToken

```tsx
import { imTokenWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### ioPay кошелек

```tsx
import { iopayWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Кая кошелек

```tsx
import { kaiaWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Kaikas

```tsx
import { kaikasWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Kraken кошелек

```tsx
import { krakenWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Kresus кошелек

```tsx
import { kresusWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Magic Eden кошелек

```tsx
import { magicEdenWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### MEW Wallet

```tsx
import { mewWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Nova кошелек

```tsx
import { novaWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### OKX кошелек

```tsx
import { okxWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Omni

```tsx
import { omniWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### ParaSwap

```tsx
import { paraSwapWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Порту

```tsx
import { portoWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Rabby кошелек

```tsx
import { rabbyWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Rainbow

```tsx
import { rainbowWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Ramper кошелек

```tsx
import { ramperWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Готово

```tsx
import { readyWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Ronin Wallet

```tsx
import { roninWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Trust Wallet

```tsx
import { trustWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Uniswap Wallet

```tsx
import { uniswapWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Valora

```tsx
import { valoraWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Вигвам

```tsx
import { wigwamWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### xPortal

```tsx
import { xPortalWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@spectrumkit/spectrumkit/wallets';
```

#### ZilPay

```tsx
import { zilPayWallet } from '@spectrumkit/spectrumkit/wallets';
```

### Примеры

#### Заказ

Вот несколько примеров отображения различных кошельков в разном порядке.

Покажите MetaMask вместе с универсальными резервными кошельками.

```tsx
import { connectorsForWallets } from '@spectrumkit/spectrumkit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@spectrumkit/spectrumkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Показать Rainbow, MetaMask и аккаунт Base вместе с общими резервными кошельками.

```tsx
import { connectorsForWallets } from '@spectrumkit/spectrumkit';
import {
  rainbowWallet,
  metaMaskWallet,
  baseAccount,
  walletConnectWallet,
} from '@spectrumkit/spectrumkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        baseAccount,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> Напоминание: порядок массива `кошельков` определяет порядок, в котором кошельки будут отображаться в интерфейсе.

#### Несколько групп

Вы можете использовать ключ `groupName`, чтобы назвать различные группы кошельков. Это удобно, если вы хотите сообщить пользователям, какие кошельки вы рекомендуете, а также другие возможные кошельки.

Рекомендовать Rainbow и MetaMask, но также предлагать аккаунт Base вместе с общими резервными кошельками.

```tsx
import { connectorsForWallets } from '@spectrumkit/spectrumkit';
import {
  rainbowWallet,
  metaMaskWallet,
  baseAccount,
  walletConnectWallet,
} from '@spectrumkit/spectrumkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [baseAccount, walletConnectWallet],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
