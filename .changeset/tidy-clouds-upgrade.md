---
'@spectrumkit/spectrumkit': patch
---

Support wagmi 3.7 / @wagmi/connectors v8: the Porto connector is now imported
from `porto/wagmi` (wagmi no longer re-exports it), and dependency floors were
raised (`@vanilla-extract/css` 1.21.2, `@vanilla-extract/sprinkles` 1.7.1,
`@base-org/account` ~2.5.10).
