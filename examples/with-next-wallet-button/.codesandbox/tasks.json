{
  "$schema": "https://codesandbox.io/schemas/tasks.json",
  "setupTasks": ["yarn add @spectrumkit/spectrumkit@latest", "yarn build"],
  "tasks": {
    "install-dependencies": {
      "name": "Install Dependencies",
      "command": "yarn add @spectrumkit/spectrumkit@latest"
    },
    "start-app": {
      "name": "Run Dev Server",
      "command": "yarn dev",
      "runAtStart": true
    }
  }
}
